export type {
  AddMessageRequest,
  CreateTicketRequest,
  Ticket,
  TicketStatistics,
  UpdateStatusRequest,
  UpdateTicketRequest,
} from '../../../types/support';

export type DeleteTicketResponse = {
  message: string;
};

